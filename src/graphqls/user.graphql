type Query {
    allUsers: [User]
    userById(id: Int!): User!
}

type Mutation {
    createAccount(
        username: String!,
        email: String!,
        firstName: String,
        lastName: String,
        bio: String
    ): User!
}
